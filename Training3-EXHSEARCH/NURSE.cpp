#include <iostream>
using namespace std;

int N, K1, K2;

void process(int k, int K1)
{
    int a[1000];
    if ()
        {
            process(k + 1, K1 + 1);

        } while (K1 != K2);
};

int main()
{
    cin >> n >> k1 >> k2;
    Try(1, 0);
    cout << ans;

    return 0;
}
