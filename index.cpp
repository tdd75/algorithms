#include <iostream>
#include <vector>
using namespace std;

// vector<int, int> a;
int main()
{
    // a.push_back((1, 2));
    // a.push_back((2, 5));
    // a.push_back((3, 6));
    // a.push_back((4, 7));

    // cout << a.size();

    cout << "size";
}